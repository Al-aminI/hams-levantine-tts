# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# model artifacts / checkpoints (publish via HF, not git)
checkpoints/
artifacts/
*.onnx
*.plan
*.pt
*.ckpt

# data (corpora are large; keep only the committed eval set)
data/manifests/
data/corpora/

# espeak bulk renders + MOS forms (we keep the NEURAL samples in samples/neural/ and the
# results JSONs — asr/utmos/benchmark — which carry the real measured numbers)
samples/eval/
samples/mos/

# LaTeX intermediates (keep paper.tex + paper.pdf)
*.aux
*.out
*.bbl
*.blg
*.toc
*.synctex.gz

# os / editor
.DS_Store
.idea/
.vscode/
*.log

# --- v2 additions ---
vendor/                    # cloned BigVGAN + tectonic binary (fetch separately)
hf_release/                # HF upload staging (large checkpoint)
paper_assets/synth/        # per-system synthesized wavs
paper/*.aux
paper/*.log
paper/fonttest.*
# listen pages (audio is large; regenerate with scripts)
samples_listen/
samples_texture/
samples_texture_ab/
samples_bigvgan/
samples_final/
samples_robustness/
# extracted corpora + big manifests (fetch from HF dataset)
data/lahgtna/
data/lahgtna_24k/
data/manifests_24k/
data/manifests_refined/
data/manifests_all_refined/
data/manifests/
data/corpora/
eval_out/
data/manifests_smoke/
data/lahgtna/
paper/_p*.png
